# Priority fixes (2026-09-15)

Status 2026-09-15 evening: rows 1, 2, 3, 8, 9, and 12's first half are done (0.5.3 gave rules ids; 0.5.6 fixed structlint). Rows 4, 5, 6, 7, 10, 11, and the overlay check remain.

What is left after the Codex review of 2026-09-15 (`docs/codex-review-2026-09-15.md`) and the two releases that answered it (0.5.1, the correctness bugs; 0.5.2, the rule fixes and the counted calibration). Fixes only; features are a separate list. Ranked by what a wrong answer costs, then by size. Each row names its evidence so the fix can be tested before it is believed.

| Rank | Fix | Where | Evidence | Size |
|---|---|---|---|---|
| 1 | The two-beat check calls two sentences parallel on character length and capitalisation alone. It needs a syntactic test: matching openers, matching negation, matching clause shape. | `structlint.py:260` | "The meeting starts at nine. Lunch follows at noon." fires as a two-beat. | half a day |
| 2 | Any prose line holding a URL, a citation marker, a long quotation, or a field label is dropped whole, so a staccato run beside a link is invisible. Mask the span, not the line. | `structlint.py:220` | "See https://example.com. It failed. It broke. It stopped." reports nothing. | half a day |
| 3 | The header stance check overmatches ordinary headings. | `structlint.py:65`, `:76` | "## The actual results" and "## Where the chair sits" both fire. | small |
| 4 | voicelint lints blockquoted text. A quoted passage is someone else's words and voice-rules.md exempts direct quotations; structlint already skips blockquotes, voicelint does not. One Markdown extraction layer for both tools (code, blockquotes, tables, headings, lists, directives) would end the two masking heuristics drifting apart. | `voicelint.py mask_code`, `structlint.py` | A `> quoted` line with a banned phrase is an error in voicelint and silent in structlint. | one day |
| 5 | The "more than two constructions per 100 words" rule in voice-rules.md is stated as global, but the density that fires is structlint's own count; voicelint findings never enter it. A shared runner that computes density over both tools' findings. | `structlint.py:303` | A draft with one two-beat and three soft phrases per 100 words passes both density checks. | half a day |
| 6 | The six errors the calibrated set found in the saga are real prose faults, not linter faults: three `the honest obstacle is that`, two `the honest answer`, one saga-own ban. They block the gate under `--strict` for those files. | el_loco_lobo `canon/legends/ruled-through.md:161`, `the-access-copy.md:176`, `the-woman-in-the-back-of-the-frame.md:88`, `qaraqapan.md:265`, `deliverables/novella/movement-3.md:125`, `three-masses-a-year-forever.md:66` | `voicelint --config tools/voice_config.json` over the shareable tree. | prose, one sitting |
| 7 | Three soft phrases Codex flagged as false positives on technical writing were not counted in the calibration: `a named cost`, `a named criterion`, `as a named`. Count them against the saga and one technical document, then keep or drop. | `voice_config.json` | `We assigned a named cost center.` warns. | small |
| 8 | `remove_<field>` matches an exact string, so an overlay that removes a shipped regex must restate the regex character for character, and a shipped edit silently un-removes it. Rules need stable IDs. This is the first step of the rule-object schema (ID, pattern, severity, scope, rationale, examples), which is a feature; the fix is the ID. | `voicelint.py _apply_list_ops` | The saga overlay could not have removed the old broad `honest` regex without pasting it. | one day |
| 9 | structlint's thresholds (short-sentence length, run length, interrogative rate) are constants; a downstream overlay cannot tune them, and its findings do not carry the voicelint JSON shape. | `structlint.py` | `lint-voice.sh` parses two output formats. | half a day |
| 10 | CI lints README and the cheat sheet without `--strict`; two `really` in README (lines 36 and 59) and `Density is the tell` in the cheat sheet warn on every run and are read past. Either reword or declare them with `voicelint-allow`. | `README.md`, `docs/CHEATSHEET.md` | `voicelint README.md docs/CHEATSHEET.md` reports 3 warnings. | minutes, author's call |
| 11 | The eval harness scores one rater, one run, no repetition, no agreement statistic, and no record of which model and prompt produced each draft. Item 2 of `docs/review-followups.md` still stands. | `eval/study.py`, `eval/README.md` | `manifest.json` has no model or prompt field. | two days |
| 12 | Downstream projects pin the vendored copy by sha256 but nothing checks that an overlay still validates against the base it was written for. A `--check-overlay` that loads base plus overlay, validates, and reports which `remove_` entries matched nothing. | `voicelint.py`, `sync-voicelint.sh` | A `remove_soft_phrases` entry for a phrase 0.5.2 dropped would pass silently. | small |

Rows 1 through 3 are one structlint session. Row 4 is the one that changes both tools and should come with its own tests before anything else touches masking. Row 6 is prose, not code, and belongs to the saga repo.
